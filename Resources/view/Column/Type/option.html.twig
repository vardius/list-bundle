{% if option is null %}
    <input type="checkbox"
           class="list-option-{{ option.property }}"
           name="{{ option.property }}"
           value=""/>
{% else %}
    <input type="checkbox"
           class="list-option-{{ option.property }}"
           name="{{ option.property }}"
           value="{{ entity.id }}"/>
{% endif %}